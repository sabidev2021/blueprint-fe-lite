import {DateIndoPipe} from './date-indo.pipe';

describe('DateIndoPipe', () => {
  it('create an instance', () => {
    // eslint-disable-next-line @typescript-eslint/ban-ts-comment
    // @ts-ignore
    const pipe = new DateIndoPipe();
    expect(pipe).toBeTruthy();
  });
});
